import React, { Component } from "react";

export default class Pizza extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                Pizza
            </div>
        );
    }
}